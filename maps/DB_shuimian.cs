using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DB_shuimian : MonoBehaviour
{
    //水面地板
    // Start is called before the first frame update
    void Start()
    {
        
    }

    //是否玩家 在侧边 靠墙
    bool IsPlayerHitCeWall = false;

    // Update is called once per frame
    void Update()
    {
        
    }
}
